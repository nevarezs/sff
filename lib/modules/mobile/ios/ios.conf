# Configuration file for general iOS Module settings
# Lines that start with a '#' character are comments and
# are ignored by the framework.
# Options that are not set in the configuration file (or are commented out)
# keep their default values
# Option values can be overwritten by individual module configuration files.
#
# Path to the iOS Backup
#BACKUP_DIR=/path/to/iOS/backup